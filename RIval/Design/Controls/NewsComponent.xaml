<UserControl x:Class="Ignite.Design.Controls.NewsComponent"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="clr-namespace:BlurryControls.Controls;assembly=BlurryControls"
             xmlns:controls1="clr-namespace:Ignite.Design.Controls"
             mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid x:Name="NewsGrid">
        <TextBlock HorizontalAlignment="Left" Margin="18,12,0,0" TextWrapping="Wrap" Text="Свежие новости" VerticalAlignment="Top" Height="30" Width="212" FontSize="22" FontWeight="Bold"/>
        <ScrollViewer x:Name="ScrollNews" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" CanContentScroll="True" PreviewMouseWheel="ScrollNews_PreviewMouseWheel">
            <Grid x:Name="NewsScrollGrid">
                
            </Grid>
        </ScrollViewer>
        <!--the BlurryUserControl must not be child of the defined BlurContainer!-->
    </Grid>
</UserControl>
